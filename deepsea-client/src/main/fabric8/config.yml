apiVersion: v1
kind: ConfigMap
metadata:
  name: ${project.artifactId}
  namespace: deepsea
data:
  database.host: mongodb
  database.port: 27017
  redis.host: redis
  redis.port: 6379
  client.host.address: 0.0.0.0
  client.host.port: 8080
  client.service.hostname: deepsea-client.deepsea.svc
  client.api.name: client
  